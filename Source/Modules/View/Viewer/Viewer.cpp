#include "Viewer.hpp"

Viewer::Viewer(WindowPosition position, SegmentSize segmentSize)
  : m_position { position }
  , m_segmentSize { segmentSize }
{}

Viewer::Render(sf::RenderWindow& window, const Data& data) {

}